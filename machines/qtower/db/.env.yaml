env:
  clear:
    PGADMIN_DEFAULT_EMAIL: admin@davafons.cc
  secret:
    - MARIADB_USER
    - MARIADB_PASSWORD
    - MARIADB_ROOT_PASSWORD
    - POSTGRES_USER
    - POSTGRES_PASSWORD
    - PGADMIN_DEFAULT_PASSWORD
